{
  "name": "help-files",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "set NODE_OPTIONS=--max-old-space-size=8192 && nuxt build",
    "dev": "set NODE_OPTIONS=--max-old-space-size=8192 && nuxt dev",
    "generate": "set NODE_OPTIONS=--max-old-space-size=8192 && nuxt generate",
    "preview": "set NODE_OPTIONS=--max-old-space-size=8192 && nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@vueuse/core": "^10.7.2",
    "nuxt": "3.10.3",
    "vue": "^3.4.19",
    "vue-router": "^4.3.0"
  }
}
